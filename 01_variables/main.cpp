int main(void)
{
    // Das ist eine Variable namens a. Sie hat den Typ int (Ganzzahl).
    int a;

    // So weist man der Variable einen Wert zu:
    a = 2;

    // Den Wert kann man auch direkt beim erstellen zuweisen:
    int b = 5;

    // Verschiedene Typen können verschieden Werte speichern:
    // Für Ganzzahlen verwendet man int (kurz für Integer), short, long oder long long

    int i = 1;
    short s = 2;
    long l = 3;
    long long ll = 4;

    // Für Kommazahlen verwendet man float (floating point) oder double (double precision)

    float f = 2.5;
    double d = 5.2;

    // Für logische Werte (true oder false) verwendet man bool (Boolean)

    bool b = true;

    // Für Buchstaben verwendet man char

    char c = 'C';

    // Buchstaben schreibt man in einzelne Anführungszeichen.
    // Es gibt ein paar Sonderbuchstaben:

    // Ein "enter" bzw. "newline"
    char newline = '\n';

    // Ein "tab"
    char tab = '\t';

    // Für ein einzelnes Backslash verwendet man '\\'
    char backslash = '\\';

    // TODO: Am Anfang der main()-Funktion wurden die Variablen int a und int b erstellt.
    // Erstelle jetzt vier variablen für Summe, Differenz, Produkt und Quotient von a
    // und b und wähle jeweils einenen passenden Datentypen

    // ...

    // TODO: Recherchiere den Unterschied zwischen int, short, long und long long so wie float und double.
}